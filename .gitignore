node_modules/
*.log
.DS_Store
# media lives on the Pi's HDD, never in the repo
api/media/
*.mp3

# SF Pro Display is © Apple — don't redistribute binaries publicly.
# Run web/assets/fonts/fetch-fonts.sh to generate them locally / on the server.
web/assets/fonts/*.woff2
